{
  "name": "docker-example",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "author": "",
  "license": "ISC",
  "scripts": {
    "docker-run": "docker run -p 5000:5000 --name feedback-app -v feedback:/app/feedback -v $(pwd):/app -d feedback-node:volumes",
    "docker-start": "docker start feedback-app",
    "docker-stop": "docker stop feedback-app",
    "docker-remove": "docker rm feedback-app",
    "docker-build": "docker build -t feedback-node:1.0.0 ."
  },
  "dependencies": {
    "@sentry/node": "^10.1.0",
    "body-parser": "^1.19.0",
    "dotenv": "^17.2.1",
    "express": "^4.17.1"
  }
}
